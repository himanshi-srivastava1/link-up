{
  "name": "link-up-server",
  "version": "1.0.0",
  "description": "real-time chat application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && cd ../client && npm install && npm run build",
    "build:prod": "cd ../client && npm install && npm run build",
    "docker:build": "docker build -t linkup-server .",
    "docker:run": "docker run -p 3001:3001 --env-file .env.production linkup-server"
  },
  "author": "Himanshi Srivastava",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.8.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.1",
    "express-validator": "^7.3.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.0.2",
    "nodemailer": "^8.0.3",
    "nodemon": "^3.1.11",
    "socket.io": "^4.8.3"
  }
}